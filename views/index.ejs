<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Index</title>
  </head>
  <body>
        <% if (locals.user) { %>
          <h1>Welcome <%= `${user.first_name} ${user.last_name}` %> to the app</h1>
        <% } %>

      <% if (locals.user) { %>  
        <a href="">Create a new Message</a> 
        <a href="/auth/logout">Logout</a> 
        <% } %>
    
    
  </body>
</html>
